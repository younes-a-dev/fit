import 'package:flutter/material.dart';

class SingleExerciseDetailScreen extends StatelessWidget {
  const SingleExerciseDetailScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        body: Column(
      children: [
        Text('younes'),
        Text('younes'),
        Text('younes'),
        Text('younes'),
        Row(
          children: [
            Text('younes'),
            Text('younes'),
            Text('younes'),
          ],
        ),
      ],
    ));
  }
}
